import test from 'node:test'; import assert from 'node:assert/strict'; import {Store,canTransition} from '../src/domain/core.js';
test('business isolation and job creation',()=>{const s=new Store();s.createBusiness({id:'arktrov',name:'ARKTROV'});assert.throws(()=>s.createJob({business_id:'zhem',format:'SHORT'}),/BUSINESS_NOT_FOUND/);const j=s.createJob({business_id:'arktrov',format:'SHORT',idempotency_key:'k'});assert.equal(j.current_state,'IDEA_CREATED');assert.equal(s.createJob({business_id:'arktrov',format:'SHORT',idempotency_key:'k'}).id,j.id)});
test('state transitions are centralized and audited',()=>{const s=new Store();s.createBusiness({id:'b',name:'B'});const j=s.createJob({business_id:'b',format:'BOTH'});s.transition(j.id,'IDEA_CREATED','DISCOVERY_COMPLETE',{actor_type:'test'});assert.equal(s.data.transitions.length,2);assert.throws(()=>s.transition(j.id,'DISCOVERY_COMPLETE','PUBLISHED'),/INVALID_TRANSITION/);assert.throws(()=>s.transition(j.id,'IDEA_CREATED','RESEARCH_PENDING'),/STALE_STATE/);assert.equal(canTransition('RESEARCH_PENDING','RESEARCH_COMPLETE'),true)});

test('artifact versions are immutable',()=>{const s=new Store();s.createBusiness({id:'b',name:'B'});const a=s.addArtifact({business_id:'b',logical_name:'voice',content_hash:'h1',storage_ref:'obj'});assert.equal(a.version,1);assert.throws(()=>s.addArtifact({business_id:'b',logical_name:'voice',version:1,content_hash:'h2',storage_ref:'obj2'}),/IMMUTABLE_ARTIFACT_VERSION/);});

test('stale state version rejected',()=>{const s=new Store();s.createBusiness({id:'b',name:'B'});const j=s.createJob({business_id:'b',format:'SHORT'});assert.throws(()=>s.transition(j.id,'IDEA_CREATED','DISCOVERY_COMPLETE',{expected_state_version:99}),/STALE_STATE_VERSION/);});
